import { Rating as MRating } from '@mui/material';
import withUIComponent from '../with-ui-component';

export * from './Attribute';
export * from './config';
export const Rating = withUIComponent(MRating);
