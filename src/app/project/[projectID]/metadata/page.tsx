// sections
import { MetadataView } from 'src/sections/metadata/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Metadata',
};

export default function MetadataPage() {
  return <MetadataView />;
}
