import {Box, config as BoxConfig} from './Box';
import {Typography, config as TypographyConfig} from './Typography';
import { Button, config as ButtonConfig} from './Button';
import { TextField, config as TextFieldConfig, TextFieldAttribute } from './TextField';
import { Image, config as ImageConfig, ImageAttribute } from './Image';
import { Iconify } from './Iconify';
import { Rating, config as RatingConfig, Attribute as RatingAttribute } from './Rating';
import { Switch, config as SwitchConfig, Attribute as SwitchAttribute } from './Switch';
import { Checkbox, config as CheckboxConfig, Attribute as CheckboxAttribute } from './Checkbox';
import { Radio, config as RadioConfig, Attribute as RadioAttribute } from './Radio';
import { Slider, config as SliderConfig, Attribute as SliderAttribute } from './Slider';
import { DatePicker, config as DatePickerConfig, Attribute as DatePickerAttribute } from './DatePicker';

export const UILib = {
  Box,
  Typography,
  Button,
  TextField,
  Image,
  Iconify,
  Rating,
  Switch,
  Checkbox,
  Radio,
  Slider,
  DatePicker,
};
export {
  Box,
  Typography,
  Button,
  TextField,
  Image,
  Iconify,
  Rating,
  Switch,
  Checkbox,
  Radio,
  Slider,
  DatePicker,
};
export const configList = {
  BoxConfig,
  TypographyConfig,
  ButtonConfig,
  TextFieldConfig,
  ImageConfig,
  RatingConfig,
  SwitchConfig,
  CheckboxConfig,
  RadioConfig,
  SliderConfig,
  DatePickerConfig,
}

export const attributeList = {
  TextFieldAttribute,
  ImageAttribute,
  RatingAttribute,
  SwitchAttribute,
  CheckboxAttribute,
  RadioAttribute,
  SliderAttribute,
  DatePickerAttribute,
};
