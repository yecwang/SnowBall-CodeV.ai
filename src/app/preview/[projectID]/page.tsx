// sections
import { PreviewView } from 'src/sections/preview/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Preview',
};

export default function Pages() {
  return <PreviewView />;
}
