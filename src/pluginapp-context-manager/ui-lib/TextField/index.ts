import { TextField as MTextField } from '@mui/material';
import withUIComponent from '../with-ui-component';

export * from './config';
export * from './Attribute';

export const TextField = withUIComponent(MTextField);
